from matplotlib.backends.backend_qtagg import FigureCanvas
from matplotlib.figure import Figure

from ui_form import metrics, votes

# points from file, numb of neighbours (k), kind of metric, kind of voting, user point x, user point y

def knn(points: list, nofNeighb: int, metric, voting, usrptx: float, usrpty: float):
	# TODO calculate KNN stuff

	# TODO return list of indexes in filepoints, which should be marked as neighbours on graph (black border)
	pass