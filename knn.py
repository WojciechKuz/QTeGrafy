from matplotlib.backends.backend_qtagg import FigureCanvas
from matplotlib.figure import Figure

metricType=["euclid","city"]
voteType=["simple", "inverseSqareDist"]


def knn(points: list, nofNeighb: int, metric, voting):
	# TODO calculate stuff

	# TODO return list of indexes in filepoints, which should be marked as neighbours on graph (black border)
	pass